# Quarto
/.quarto/
*_files/
*_cache/
/.luarc.json

# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/

# Note: /docs is intentionally NOT ignored — GitHub Pages serves the site
# from that folder, so the rendered output must be committed.

**/*.quarto_ipynb

# Full-resolution photo originals: kept on disk, not published.
# The web-sized copy in assets/img/ is what the site actually serves.
assets/img/originals/
